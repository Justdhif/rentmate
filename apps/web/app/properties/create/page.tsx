import { CreatePropertyView } from '@/components/properties/CreatePropertyView';

export const metadata = {
  title: 'Tambah Properti Baru - RentMate',
  description: 'Daftarkan properti kost baru dengan peta lokasi interaktif.',
};

export default function CreatePropertyPage() {
  return <CreatePropertyView />;
}
